// Basic (scalar) parameter use outside of optimisation.
//param({name: 'x'});
//console.log(getParams());
//getParams().x.print();
//param({name: 'x'}).print();

// ==================================================

var model = function() {
  var p = param({name: "mu"});
  console.log(p);
  return p;
};

Optimize({model, steps: 1});
assert.ok(false);

// ==================================================

// Basic functionality of Gaussian distribution

var mu = 0.0;
var sigma = 1.0;
var d = Gaussian({mu, sigma});
//display(d.score(2.0));
d.score(2.0).print();
display(d.sample());
display('----------------------------------------');

var mu = tf.variable(tf.scalar(0.0));
var sigma = tf.variable(tf.scalar(1.0));
var d = Gaussian({mu, sigma});
//console.log(d.score(2.0));
d.score(2.0).print();
display(d.sample());
display('----------------------------------------');

// ==================================================
